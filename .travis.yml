language: ruby
rvm:
  - 1.9.3
install: gem install fpm-cookery
script:
  - find ./ -maxdepth 1 -type d -exec sh -c 'cd "$0" && sudo fpm-cook' {} \;
#  - cd amos && fpm-cook

# deb packages are created under ./*/pkg/*.deb
