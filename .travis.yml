language: ruby
sudo: required
rvm:
  - 1.9.3
before_install: 
  - sudo $(which rvm) rubygems current
  - sudo $(which gem) install fpm-cookery
script:
  - cd cufflinks && sudo fpm-cook clean && sudo fpm-cook
#  - find ./ -maxdepth 1 -type d -exec sh -c 'cd "$0" && fpm-cook' {} \;
#  - cd amos && fpm-cook

# deb packages are created under ./*/pkg/*.deb
